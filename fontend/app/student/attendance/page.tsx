import { StudentAttendancePage } from "@/components/student/student-attendance";

/** C-ST-03 and C-ST-04 — the breakdown and the calendar share one payload. */
export default function StudentAttendanceRoute() {
  return <StudentAttendancePage />;
}
