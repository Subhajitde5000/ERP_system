import { ExamControllerResultsPage } from "@/components/exam-controller/exam-controller-results";

/** C-EC-07 — compile results into a publication. */
export default function ExamControllerResultsRoute() {
  return <ExamControllerResultsPage />;
}
