import { CoordinatorNoticeComposerPage } from "@/components/coordinator/coordinator-notice-composer";

/** C-AC-08 — post an academic notice (class-scoped only per §4.5). */
export default function CoordinatorNewNoticeRoute() {
  return <CoordinatorNoticeComposerPage />;
}
