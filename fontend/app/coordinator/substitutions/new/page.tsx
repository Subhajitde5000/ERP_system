import { CoordinatorSubstitutionFormPage } from "@/components/coordinator/coordinator-substitution-form";

/** C-AC-06 — add a substitution. */
export default function CoordinatorNewSubstitutionRoute() {
  return <CoordinatorSubstitutionFormPage />;
}
