import { CoordinatorSubstitutionBoardPage } from "@/components/coordinator/coordinator-substitution-board";

/** C-AC-05 — substitution board, served by the live API. */
export default function CoordinatorSubstitutionsRoute() {
  return <CoordinatorSubstitutionBoardPage />;
}
