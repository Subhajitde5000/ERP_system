import { CoordinatorCalendarPage } from "@/components/coordinator/coordinator-calendar";

/** C-AC-07 — academic calendar (holidays, events, exam weeks, term markers). */
export default function CoordinatorCalendarRoute() {
  return <CoordinatorCalendarPage />;
}
