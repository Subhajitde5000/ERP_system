import { CoordinatorDashboardPage } from "@/components/coordinator/coordinator-dashboard";

/** C-AC-01 — live academic-operations dashboard. */
export default function CoordinatorDashboardRoute() {
  return <CoordinatorDashboardPage />;
}
