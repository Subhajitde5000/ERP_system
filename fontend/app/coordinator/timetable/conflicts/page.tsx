import { CoordinatorConflictsPage } from "@/components/coordinator/coordinator-conflicts";

/** C-AC-04 — teacher / room double-booking report. */
export default function CoordinatorConflictsRoute() {
  return <CoordinatorConflictsPage />;
}
