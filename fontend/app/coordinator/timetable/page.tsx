import { CoordinatorTimetablePage } from "@/components/coordinator/coordinator-timetable";

/** C-AC-02 — institution-wide weekly timetable builder. */
export default function CoordinatorTimetableRoute() {
  return <CoordinatorTimetablePage />;
}
