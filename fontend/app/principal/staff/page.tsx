import { PrincipalDirectoryPage } from "@/components/principal/directory";

/** C-PR-05 — live, read-only institution-wide staff directory. */
export default function PrincipalStaffPage() {
  return <PrincipalDirectoryPage kind="staff" />;
}
