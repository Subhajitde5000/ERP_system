"use client"; import {useEffect} from "react"; import {useRouter} from "next/navigation"; import {InstitutionAuthProvider,useInstitutionAuth} from "@/hooks/use-institution-auth"; import {HostelShell} from "@/components/hostel/hostel-shell";
function Gate({children}:{children:React.ReactNode}){const{isAuthenticated,isLoading}=useInstitutionAuth();const router=useRouter();useEffect(()=>{if(!isLoading&&!isAuthenticated)router.replace("/login")},[isAuthenticated,isLoading,router]);if(isLoading||!isAuthenticated)return <div className="flex min-h-screen items-center justify-center">Loading hostel…</div>;return <HostelShell>{children}</HostelShell>} export default function Layout({children}:{children:React.ReactNode}){return <InstitutionAuthProvider><Gate>{children}</Gate></InstitutionAuthProvider>}
